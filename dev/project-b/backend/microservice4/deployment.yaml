apiVersion: apps/v1
kind: Deployment
metadata:
  name: microservice4-backend
  namespace: backend
  labels:
    app: microservice4-backend
spec:
  replicas: 2
  selector:
    matchLabels:
      app: microservice4-backend
  template:
    metadata:
      labels:
        app: microservice4-backend
    spec:
      containers:
      - name: nginx
        image: nginx:1.25-alpine # Image dapat diupdate sesuai kebutuhan
        ports:
        - containerPort: 80
